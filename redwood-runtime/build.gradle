import app.cash.redwood.buildsupport.KmpTargets

apply plugin: 'org.jetbrains.kotlin.multiplatform'
apply plugin: 'org.jetbrains.kotlin.plugin.serialization'
apply plugin: 'com.android.library'

redwoodBuild {
  // TODO Re-enable the Compose compiler. This is not strictly required because we only use the
  //  @Immutable and @Stable annotations, but it's a best-practice to still run it and get the
  //  generated field on the type.
  // composeCompiler()
  publishing()
}

kotlin {
  KmpTargets.addAllTargets(project)

  sourceSets {
    commonMain {
      dependencies {
        implementation libs.jetbrains.compose.runtime
        implementation libs.kotlinx.serialization.core
      }
    }
    commonTest {
      dependencies {
        implementation libs.kotlin.test
        implementation libs.assertk
      }
    }
  }
}

android {
  namespace 'app.cash.redwood.runtime'
}

spotless {
  kotlin {
    targetExclude(
      // Apache 2-licensed files from AOSP.
      "src/commonMain/kotlin/app/cash/redwood/Modifier.kt",
    )
  }
}
