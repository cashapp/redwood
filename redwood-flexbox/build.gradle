import app.cash.redwood.buildsupport.KmpTargets

apply plugin: 'org.jetbrains.kotlin.multiplatform'

redwoodBuild {
  publishing()
}

kotlin {
  KmpTargets.addAllTargets(project)

  sourceSets {
    commonTest {
      dependencies {
        implementation libs.kotlin.test
        implementation libs.assertk
      }
    }
  }
}

spotless {
  kotlin {
    targetExclude(
      // Apache 2-licensed files from Google.
      "src/commonMain/kotlin/app/cash/redwood/flexbox/FlexContainer.kt",
      "src/commonMain/kotlin/app/cash/redwood/flexbox/FlexItem.kt",
      "src/commonMain/kotlin/app/cash/redwood/flexbox/FlexLine.kt",
      "src/commonTest/kotlin/app/cash/redwood/flexbox/FlexContainerTest.kt",
    )
  }
}
