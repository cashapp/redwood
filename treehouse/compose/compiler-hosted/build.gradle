apply plugin: 'org.jetbrains.kotlin.jvm'
apply plugin: 'com.vanniktech.maven.publish'

sourceSets {
  main {
    java.srcDir '../upstream/compose/compiler/compiler-hosted/src/main/java'
    resources.srcDir '../upstream/compose/compiler/compiler-hosted/src/main/resources'
  }
}

dependencies {
  compileOnly deps.kotlin.compiler
}
