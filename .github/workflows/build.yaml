name: build

on:
  pull_request: {}
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'

env:
  GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx6g -Dorg.gradle.daemon=false -Dkotlin.incremental=false"

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2
      - run: git submodule update --init

      - uses: gradle/wrapper-validation-action@v1
      - uses: actions/setup-java@v2.3.1
        with:
          distribution: 'zulu'
          java-version: 14

      - run: ./gradlew -p treehouse build dokkaHtmlMultiModule --parallel

      - name: Build samples
        run: ./gradlew build --parallel

      - name: Build Xcode samples
        run: |
          cd samples/counter/ios/app
          pod install
          xcodebuild -workspace CounterApp.xcworkspace -scheme CounterApp -destination 'platform=iOS Simulator,name=iPhone 12,OS=latest'

      - run: ./gradlew -p treehouse publish
        if: ${{ github.ref == 'refs/heads/trunk' && github.repository == 'cashapp/treehouse' }}
        env:
          ORG_GRADLE_PROJECT_SONATYPE_NEXUS_USERNAME: ${{ secrets.SONATYPE_NEXUS_USERNAME }}
          ORG_GRADLE_PROJECT_SONATYPE_NEXUS_PASSWORD: ${{ secrets.SONATYPE_NEXUS_PASSWORD }}

      - name: Deploy docs to website
        if: ${{ github.ref == 'refs/heads/trunk' && github.repository == 'cashapp/treehouse' }}
        uses: JamesIves/github-pages-deploy-action@releases/v3
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: site
          FOLDER: treehouse/build/dokka/htmlMultiModule
          TARGET_FOLDER: docs/latest/
          CLEAN: true

  windows-publish:
    runs-on: windows-latest
    if: ${{ github.ref == 'refs/heads/trunk' && github.repository == 'cashapp/treehouse' }}

    needs:
      - build

    steps:
      - uses: actions/checkout@v2
      - run: git submodule update --init

      - uses: gradle/wrapper-validation-action@v1
      - uses: actions/setup-java@v2.3.1
        with:
          distribution: 'zulu'
          java-version: 14

      - run: ./gradlew -p treehouse publishMingwX64PublicationToMavenRepository
        env:
          ORG_GRADLE_PROJECT_SONATYPE_NEXUS_USERNAME: ${{ secrets.SONATYPE_NEXUS_USERNAME }}
          ORG_GRADLE_PROJECT_SONATYPE_NEXUS_PASSWORD: ${{ secrets.SONATYPE_NEXUS_PASSWORD }}
