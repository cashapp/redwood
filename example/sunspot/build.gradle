apply plugin: 'org.jetbrains.kotlin.multiplatform'

kotlin {
  jvm()
  js {
    browser()
  }

  sourceSets {
    commonMain {
      dependencies {
        api project(':treehouse-schema-annotations')
      }
    }
  }
}
