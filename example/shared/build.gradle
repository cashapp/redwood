apply plugin: 'org.jetbrains.kotlin.multiplatform'
apply plugin: 'com.android.library'

kotlin {
  android()
  jvm()

  sourceSets {
    commonMain {
      dependencies {
        api project(':example:sunspot:compose')
      }
    }
  }
}

dependencies {
  kotlinPlugin deps.composeCompiler
}
