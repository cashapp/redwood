apply plugin: 'org.jetbrains.kotlin.js'

archivesBaseName = 'example'

kotlin {
  target {
    browser()
  }

  sourceSets {
    main {
      dependencies {
        implementation project(':example:sunspot:client')
        implementation deps.kotlin.coroutines.core
      }
    }
  }
}
