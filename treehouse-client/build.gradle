apply plugin: 'org.jetbrains.kotlin.multiplatform'
apply plugin: 'app.cash.exhaustive'

kotlin {
  js {
    browser()
  }
  jvm()

  sourceSets {
    commonMain {
      dependencies {
        api project(':treehouse-protocol')

        // TODO remove once https://github.com/cashapp/exhaustive/issues/6 is fixed.
        compileOnly deps.exhaustiveAnnotation
      }
    }
  }
}
