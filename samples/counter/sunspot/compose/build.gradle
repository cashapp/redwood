apply plugin: 'org.jetbrains.kotlin.multiplatform'
apply plugin: 'app.cash.redwood.generator.compose'

archivesBaseName = 'sunspot-compose'

kotlin {
  iosArm64()
  iosX64()
  iosSimulatorArm64()

  js {
    browser()
  }
  jvm()

  sourceSets {
    commonMain {
      dependencies {
        implementation(projects.samples.counter.sunspot.widget)
      }
    }
  }
}

redwoodSchema {
  source = projects.samples.counter.sunspot
  type = 'example.sunspot.Sunspot'
}
