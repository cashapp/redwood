apply plugin: 'org.jetbrains.kotlin.multiplatform'
apply plugin: 'app.cash.redwood.generator.compose.protocol'

kotlin {
  ios()
  js {
    browser()
  }
  jvm()

  sourceSets {
    commonMain {
      dependencies {
        implementation(projects.counter.sunspot.widget)
      }
    }
  }
}

redwoodSchema {
  source = projects.counter.sunspot
  type = 'example.sunspot.Sunspot'
}
