apply plugin: 'org.jetbrains.kotlin.multiplatform'
apply plugin: 'app.cash.treehouse.schema.compose.protocol'

kotlin {
  ios()
  js {
    browser()
  }
  jvm()

  sourceSets {
    commonMain {
      dependencies {
        implementation(project(':samples:counter:sunspot:widget'))
      }
    }
  }
}

treehouseSchema {
  source = project(':samples:counter:sunspot')
  type = 'example.sunspot.Sunspot'
}
