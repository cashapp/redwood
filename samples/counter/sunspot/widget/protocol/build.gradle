apply plugin: 'org.jetbrains.kotlin.multiplatform'
apply plugin: 'app.cash.redwood.schema.widget.protocol'

kotlin {
  ios()
  js {
    browser()
  }
  jvm()

  sourceSets {
    commonMain {
      dependencies {
        api(projects.counter.sunspot.widget)
      }
    }
  }
}

redwoodSchema {
  source = project(':counter:sunspot')
  type = 'example.sunspot.Sunspot'
}
