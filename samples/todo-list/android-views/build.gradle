apply plugin: 'com.android.application'
apply plugin: 'org.jetbrains.kotlin.android'
apply plugin: 'app.cash.treehouse'

dependencies {
  implementation project(':samples:todo-list:schema:widget')
  implementation project(':samples:todo-list:presenters')
  implementation deps.kotlinx.coroutines.android
  implementation deps.androidx.appCompat
  implementation deps.materialDesign
  implementation 'app.cash.treehouse:treehouse-widget-zipline'
  implementation 'app.cash.treehouse:treehouse-zipline'
}

android {
  buildFeatures {
    viewBinding true
  }
}
