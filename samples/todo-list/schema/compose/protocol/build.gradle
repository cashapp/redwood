apply plugin: 'org.jetbrains.kotlin.multiplatform'
apply plugin: 'app.cash.treehouse.schema.compose.protocol'

kotlin {
  ios()
  jvm()

  sourceSets {
    commonMain {
      dependencies {
        api(project(':samples:todo-list:schema:widget'))
      }
    }
  }
}

treehouseSchema {
  source = project(':samples:todo-list:schema')
  type = 'example.schema.Todo'
}
