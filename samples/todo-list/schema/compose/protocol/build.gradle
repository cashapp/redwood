apply plugin: 'org.jetbrains.kotlin.multiplatform'
apply plugin: 'app.cash.redwood.schema.compose.protocol'

kotlin {
  ios()
  jvm()

  sourceSets {
    commonMain {
      dependencies {
        api(projects.todoList.schema.widget)
      }
    }
  }
}

redwoodSchema {
  source = project(':todo-list:schema')
  type = 'example.schema.Todo'
}
