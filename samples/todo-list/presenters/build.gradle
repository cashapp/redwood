apply plugin: 'org.jetbrains.kotlin.multiplatform'
apply plugin: 'app.cash.redwood'

kotlin {
  ios()
  jvm()

  sourceSets {
    commonMain {
      dependencies {
        api projects.todoList.schema.compose
      }
    }
  }
}
